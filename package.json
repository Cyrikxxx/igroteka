{
  "name": "alias-online",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --raw -n web,ws -c green,cyan \"npm:dev:web\" \"npm:dev:ws\"",
    "dev:web": "cd apps/web && next dev",
    "dev:ws": "cd apps/ws && tsx watch src/index.ts",
    "dev:web-only": "npm run dev --workspace @alias/web",
    "build": "npm run build:web",
    "build:web": "prisma generate && npm run build --workspace @alias/web",
    "start": "npm run start --workspace @alias/web",
    "lint": "npm run lint --workspace @alias/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:deploy": "prisma migrate deploy",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "setup:local": "docker compose up -d --wait && prisma migrate deploy && prisma db seed"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prisma": "^6.19.3",
    "tsx": "^4.19.2"
  }
}
